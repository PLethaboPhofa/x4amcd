## Execution:
dotnet run


## Notes
1, Dynamic state is when the state is defined by a function rather than a constant value
2, Memory must be a linked list where each link is 1 time step. Ana gent who knows the full game state would 
have a perfect replica of the world in their memory

3. Goals should be an object which contains sub objects that further describe the goaal
